# Security

Do not commit real mainframe extracts, job names, dataset names, customer identifiers, transaction payloads, credentials, connection strings, ticket IDs, production screenshots, or operational logs.

Use synthetic workload data only for public portfolio proof.
